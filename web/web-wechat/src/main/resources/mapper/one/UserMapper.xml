<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- Mybatis mapper for User  Auto generated by fms-mybatis-generator -->
<mapper namespace="com.arc.wechat.mapper.one.UserMapper">
	<resultMap id="user" type="com.arc.model.domain.common.User">
		<id property="id" column="u_id" javaType="long" jdbcType="BIGINT" />
		<result property="nickname" column="u_nickname" javaType="string" jdbcType="VARCHAR" />
		<result property="avatar" column="u_avatar" javaType="string" jdbcType="VARCHAR" />
		<result property="level" column="u_level" javaType="int" jdbcType="INTEGER" />
		<result property="gender" column="u_gender" javaType="int" jdbcType="INTEGER" />
		<result property="birthday" column="u_birthday" javaType="java.util.Date" jdbcType="TIMESTAMP" />
		<result property="sign" column="u_sign" javaType="string" jdbcType="VARCHAR" />
		<result property="userType" column="u_user_type" javaType="int" jdbcType="INTEGER" />
	</resultMap>

	<sql id="sqluserColumns">
		u_.id AS u_id,
		u_.nickname AS u_nickname,
		u_.avatar AS u_avatar,
		u_.level AS u_level,
		u_.gender AS u_gender,
		u_.birthday AS u_birthday,
		u_.sign AS u_sign,
		u_.user_type AS u_user_type
	</sql>

	<select id="get" parameterType="long" resultMap="user">
		SELECT <include refid="sqluserColumns" />
        FROM user u_
        WHERE u_.id = #{id,jdbcType=BIGINT}
	</select>

<!--	<insert id="save" parameterType="com.del.model.common.user" useGeneratedKeys="true" keyProperty="id">
		INSERT INTO user (
			nickname,
			avatar,
			`level`,
			gender,
			birthday,
			sign,
			user_type
		)
        VALUES (
			#{nickname,jdbcType=VARCHAR},
			#{avatar,jdbcType=VARCHAR},
			#{level,jdbcType=INTEGER},
			#{gender,jdbcType=INTEGER},
			#{birthday,jdbcType=TIMESTAMP},
			#{sign,jdbcType=VARCHAR},
			#{userType,jdbcType=INTEGER}
		)
	</insert>

	<update id="update" parameterType="com.del.model.common.user">
		UPDATE user
		<set>
			<if test="nickname != null and nickname != ''">nickname = #{nickname,jdbcType=VARCHAR},</if>
			<if test="avatar != null and avatar != ''">avatar = #{avatar,jdbcType=VARCHAR},</if>
			<if test="level != null">`level` = #{level,jdbcType=INTEGER},</if>
			<if test="gender != null">gender = #{gender,jdbcType=INTEGER},</if>
			<if test="birthday != null">birthday = #{birthday,jdbcType=TIMESTAMP},</if>
			<if test="sign != null and sign != ''">sign = #{sign,jdbcType=VARCHAR},</if>
			<if test="userType != null">user_type = #{userType,jdbcType=INTEGER},</if>
		</set>
		WHERE id = #{id,jdbcType=BIGINT}
	</update>
	-->
</mapper>
