<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- Mybatis mapper for user_auths  Auto generated by fms-mybatis-generator -->
<mapper namespace="com.security.mapper.UserAuthMapper">
    <resultMap id="user_auths" type="com.arc.model.domain.system.UserAuth">
        <id property="id" column="uauth_id" javaType="long" jdbcType="BIGINT"/>
        <result property="userId" column="uauth_user_id" javaType="long" jdbcType="BIGINT"/>
        <result property="identityType" column="uauth_identity_type" javaType="string" jdbcType="VARCHAR"/>
        <result property="identifier" column="uauth_identifier" javaType="string" jdbcType="VARCHAR"/>
        <result property="credential" column="uauth_credential" javaType="string" jdbcType="VARCHAR"/>
        <result property="state" column="uauth_state" javaType="int" jdbcType="INTEGER"/>
    </resultMap>

    <sql id="sqluser_authsColumns">
		uauth_.id AS uauth_id,
		uauth_.user_id AS uauth_user_id,
		uauth_.identity_type AS uauth_identity_type,
		uauth_.identifier AS uauth_identifier,
		uauth_.credential AS uauth_credential,
		uauth_.state AS uauth_state
	</sql>

    <select id="get" parameterType="long" resultMap="user_auths">
        SELECT
        <include refid="sqluser_authsColumns"/>
        FROM user_auths uauth_
        WHERE uauth_.id = #{id,jdbcType=BIGINT}
    </select>

    <select id="list" resultMap="user_auths">
        SELECT
        <include refid="sqluser_authsColumns"/>
        FROM user_auths uauth_
    </select>

    <insert id="save" parameterType="com.arc.model.domain.system.UserAuth" useGeneratedKeys="true" keyProperty="id">
		INSERT INTO user_auths (
			user_id,
			identity_type,
			identifier,
			credential,
			state
		)
        VALUES (
			#{userId,jdbcType=BIGINT},
			#{identityType,jdbcType=VARCHAR},
			#{identifier,jdbcType=VARCHAR},
			#{credential,jdbcType=VARCHAR},
			#{state,jdbcType=INTEGER}
		)
	</insert>

    <update id="update" parameterType="com.arc.model.domain.system.UserAuth">
        UPDATE user_auths
        <set>
            <if test="userId != null">user_id = #{userId,jdbcType=BIGINT},</if>
            <if test="identityType != null and identityType != ''">identity_type = #{identityType,jdbcType=VARCHAR},
            </if>
            <if test="identifier != null and identifier != ''">identifier = #{identifier,jdbcType=VARCHAR},</if>
            <if test="credential != null and credential != ''">credential = #{credential,jdbcType=VARCHAR},</if>
            <if test="state != null">state = #{state,jdbcType=INTEGER},</if>
        </set>
        WHERE id = #{id,jdbcType=BIGINT}
    </update>

</mapper>
