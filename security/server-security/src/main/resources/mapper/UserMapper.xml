<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- Mybatis mapper for user  Auto generated by fms-mybatis-generator -->
<mapper namespace="com.security.mapper.UserMapper">
    <resultMap id="user" type="com.arc.model.domain.system.SsoUser">
        <id property="id" column="u_id" javaType="long" jdbcType="BIGINT"/>
        <result property="nickname" column="u_nickname" javaType="string" jdbcType="VARCHAR"/>
        <result property="avatar" column="u_avatar" javaType="string" jdbcType="VARCHAR"/>
        <result property="sign" column="u_sign" javaType="int" jdbcType="INTEGER"/>
    </resultMap>

    <sql id="ssoUserColumnsSQL">
		u_.id AS u_id,
		u_.nickname AS u_nickname,
		u_.avatar AS u_avatar,
		u_.sign AS u_sign
	</sql>

    <select id="get" parameterType="long" resultMap="user">
        SELECT
        <include refid="ssoUserColumnsSQL"/>
        FROM user u_
        WHERE u_.id = #{id,jdbcType=BIGINT}
    </select>

    <select id="getByUsername" parameterType="string" resultMap="user">
        SELECT
        <include refid="ssoUserColumnsSQL"/>
        FROM user u_
        WHERE u_.username = #{username,jdbcType=VARCHAR}
    </select>

    <select id="getByNickname" parameterType="string" resultMap="user">
        SELECT
        <include refid="ssoUserColumnsSQL"/>
        FROM user u_
        WHERE u_.nickname = #{nickname,jdbcType=VARCHAR}
    </select>

    <select id="list" resultMap="user">
        SELECT
        <include refid="ssoUserColumnsSQL"/>
        FROM user u_
    </select>

    <delete id="delete" parameterType="long">
    DELETE  FROM USER WHERE  id=#{id}
    </delete>

    <insert id="save" parameterType="com.arc.model.domain.system.SsoUser" useGeneratedKeys="true" keyProperty="id">
		INSERT INTO user (
			nickname,
			avatar,
			sign
		)
        VALUES (
			#{nickname,jdbcType=VARCHAR},
			#{avatar,jdbcType=VARCHAR},
			#{sign,jdbcType=INTEGER}
		)
	</insert>

    <update id="update" parameterType="com.arc.model.domain.system.SsoUser">
        UPDATE user
        <set>
            <if test="nickname != null and nickname != ''">nickname = #{nickname,jdbcType=VARCHAR},</if>
            <if test="avatar != null and avatar != ''">avatar = #{avatar,jdbcType=VARCHAR},</if>
            <if test="sign != null">sign = #{sign,jdbcType=INTEGER},</if>
        </set>
        WHERE id = #{id,jdbcType=BIGINT}
    </update>

</mapper>
